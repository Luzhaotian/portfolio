/** Public barrel for style system. Prefer importing from specific packages. */
export {
  DEFAULT_STYLE,
  STYLE_IDS,
  STYLE_PATHS,
  STYLE_REGISTRY,
  getStyleDefinition,
  isStyleMode,
  type StyleMode,
  type StyleDefinition,
} from "@/styles/registry";
